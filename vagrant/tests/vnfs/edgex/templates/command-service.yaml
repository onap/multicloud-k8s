apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: {{ .Values.service.command.name }}
  name: {{ .Values.service.command.name }}
spec:
  ports:
  - name: {{ .Values.service.command.portName }}
    port: {{ .Values.service.command.port }}
    targetPort: {{ .Values.service.command.port }}
  selector:
    io.kompose.service: {{ .Values.service.command.name }}
