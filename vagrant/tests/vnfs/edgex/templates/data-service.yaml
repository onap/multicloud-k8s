apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: {{ .Values.service.data.name }}
  name: {{ .Values.service.data.name }}
spec:
  ports:
  {{- range $key, $val := .Values.service.data.ports }}
  - name: {{ $val.portName }}
    port: {{ $val.port }}
    targetPort: {{ $val.port }}
  {{- end }}
  selector:
    io.kompose.service: {{ .Values.service.data.name }}
